<!DOCTYPE html>
<html lang="pt-br">
<head>
    <meta charset="UTF-8">
    <title>Registrar Usuário</title>
    <style>
        body { font-family: Arial, sans-serif; background: #f6f8fa; }
        .register-container {
            background: #fff;
            max-width: 340px;
            margin: 60px auto;
            padding: 32px 28px;
            border-radius: 12px;
            box-shadow: 0 8px 32px rgba(37,117,252,0.13);
        }
        h2 { color: #2575fc; margin-bottom: 18px; text-align: center; }
        input { width: 100%; margin-bottom: 14px; padding: 10px; border-radius: 6px; border: 1px solid #ccc; }
        button { width: 100%; background: #2575fc; color: #fff; border: none; border-radius: 8px; padding: 12px; font-size: 1rem; font-weight: bold; cursor: pointer; }
        button:hover { background: #1a5dcc; }
        a { display: block; text-align: center; margin-top: 12px; color: #2575fc; text-decoration: none; }
    </style>
</head>
<body>
    <div class="register-container">
        <h2>Registrar Usuário</h2>
        <form action="/register" method="POST">
            <input type="text" name="name" placeholder="Nome" required>
            <input type="email" name="email" placeholder="E-mail" required>
            <input type="password" name="password" placeholder="Senha" required>
            <button type="submit">Registrar</button>
        </form>
        <a href="/login">Já possui conta? Login</a>
    </div>
</body>
</html>
