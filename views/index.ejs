<!DOCTYPE html>
<html lang="pt-br">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title><%= titulo %></title>
    <style>
        body {
            background: linear-gradient(120deg, #6a11cb 0%, #2575fc 100%);
            min-height: 100vh;
            margin: 0;
            font-family: 'Segoe UI', Arial, sans-serif;
            display: flex;
            flex-direction: column;
            align-items: center;
            justify-content: flex-start;
        }
        .container-inicial {
            display: flex;
            flex-direction: column;
            align-items: center;
            justify-content: center;
            min-height: 100vh;
            padding: 0 16px;
            text-align: center;
        }
        .container-inicial h1 {
            font-size: 2.5rem;
            margin-bottom: 16px;
            color: #fff;
            text-shadow: 2px 2px 8px rgba(0,0,0,0.15);
        }
        .container-inicial p {
            font-size: 1.2rem;
            margin-bottom: 32px;
            color: #e0e0e0;
        }
        .container-inicial a {
            color: #2575fc;
            font-weight: bold;
            text-decoration: none;
            transition: color 0.2s;
        }
        .container-inicial a:hover {
            color: #6a11cb;
        }
    </style>
</head>
<body>
    <div class="container-inicial">
        <h1>Bem-vindo ao Task Manager</h1>
        <p>Gerencie suas tarefas de forma simples e eficiente.</p>
        <p>Acesse a interface moderna em <a href="/app">/app</a></p>
    </div>
</body>
</html>
            box-shadow: 0 2px 8px #2575fc33;
        }
        .nova-tarefa button[type="submit"] {
            background: linear-gradient(90deg, #6a11cb 0%, #2575fc 100%);
            color: #fff;
            border: none;
            border-radius: 8px;
            font-size: 1.08rem;
            font-weight: bold;
            cursor: pointer;
            padding: 12px 22px;
            box-shadow: 0 2px 8px #2575fc22;
            transition: background 0.2s, transform 0.1s;
        }
        .nova-tarefa button[type="submit"]:hover {
            background: linear-gradient(90deg, #2575fc 0%, #6a11cb 100%);
            transform: scale(1.04);
        }
        ul {
            list-style: none;
            padding: 0;
            margin: 0;
        }
        li {
            display: flex;
            align-items: center;
            margin-bottom: 16px;
            font-size: 1.13rem;
            background: #f6f8fa;
            border-radius: 10px;
            padding: 12px 16px;
            box-shadow: 0 1px 6px rgba(37,117,252,0.07);
            transition: box-shadow 0.2s, background 0.2s;
            position: relative;
            animation: fadeIn 0.5s;
        }
        li:hover {
            background: #eaf1fb;
            box-shadow: 0 2px 12px rgba(37,117,252,0.13);
        }
        .done {
            text-decoration: line-through;
            color: #b0b0b0;
            opacity: 0.7;
            font-style: italic;
            position: relative;
        }
        .done::before {
            content: '✔️ ';
            font-size: 1.1em;
            margin-right: 2px;
        }
        li form {
            margin-left: 10px;
            display: inline;
        }
        button {
            padding: 8px 16px;
            background: #2575fc;
            color: #fff;
            border: none;
            border-radius: 7px;
            font-size: 1rem;
            font-weight: bold;
            cursor: pointer;
            transition: background 0.2s, transform 0.1s;
            margin-left: 2px;
            display: flex;
            align-items: center;
            gap: 4px;
            box-shadow: 0 1px 4px #2575fc22;
        }
        button:hover {
            background: #1a5dcc;
            transform: translateY(-2px) scale(1.04);
        }
        @media (max-width: 500px) {
            .container-tarefas {
                min-width: unset;
                width: 95vw;
                padding: 18px 6vw;
            }
            li {
                flex-direction: column;
                align-items: flex-start;
                gap: 8px;
            }
            li form {
                margin-left: 0;
            }
        }
        @keyframes fadeIn {
            from { opacity: 0; transform: translateY(10px); }
            to { opacity: 1; transform: translateY(0); }
        }
        .container-inicial {
            display: flex;
            flex-direction: column;
            align-items: center;
            justify-content: center;
            min-height: 100vh;
            padding: 0 16px;
            text-align: center;
        }
        .container-inicial h1 {
            font-size: 2.5rem;
            margin-bottom: 16px;
            color: #fff;
            text-shadow: 2px 2px 8px rgba(0,0,0,0.15);
        }
        .container-inicial p {
            font-size: 1.2rem;
            margin-bottom: 32px;
            color: #e0e0e0;
        }
        .container-inicial button {
            padding: 12px 24px;
            font-size: 1.1rem;
            font-weight: bold;
            color: #fff;
            background: linear-gradient(90deg, #6a11cb 0%, #2575fc 100%);
            border: none;
            border-radius: 8px;
            cursor: pointer;
            transition: background 0.3s, transform 0.2s;
            box-shadow: 0 4px 16px rgba(37,117,252,0.2);
        }
        .container-inicial button:hover {
            background: linear-gradient(90deg, #2575fc 0%, #6a11cb 100%);
            transform: translateY(-2px);
        }
        #register-modal {
            display: none;
            position: fixed;
            top: 0;
            left: 0;
            width: 100vw;
            height: 100vh;
            background: #0008;
            align-items: center;
            justify-content: center;
        }
        #register-modal form {
            background: #fff;
            padding: 32px 24px;
            border-radius: 12px;
            min-width: 280px;
            max-width: 90vw;
            margin: auto;
            display: flex;
            flex-direction: column;
            gap: 16px;
        }
        #register-modal h2 {
            margin-bottom: 12px;
            color: #2575fc;
        }
        #register-modal input {
            padding: 10px;
            border-radius: 6px;
            border: 1px solid #ccc;
        }
        #register-modal button[type="button"] {
            background: #eee;
            color: #333;
        }
        #register-modal button[type="submit"] {
            background: #2575fc;
            color: #fff;
        }
    </style>
    <script>
        function toggleRegister() {
            const reg = document.getElementById('register-box');
            reg.style.display = reg.style.display === 'none' ? 'flex' : 'none';
        }
    </script>
</head>
<body>
    <div class="container-tarefas">
        <h1><%= titulo %></h1>
        <form class="nova-tarefa" action="/tasks/add" method="POST">
            <input type="text" name="title" placeholder="Nova tarefa" required pattern=".*@.*" title="O usuário deve ser um e-mail (conter @)">
            <button type="submit">Adicionar</button>
        </form>
        <button type="button" style="margin-top:18px; background:#fff; color:#2575fc; border:1.5px solid #2575fc; box-shadow:none;" onclick="toggleRegister()">Registrar novo usuário</button>
        <form id="register-box" action="/register" method="POST" style="display:none; flex-direction:column; gap:16px; margin-top:18px; width:100%; animation: fadeIn 0.4s;">
            <input type="text" name="nome" placeholder="Nome completo" required>
            <input type="tel" name="telefone" placeholder="Telefone" required pattern="[0-9]{10,11}" title="Digite apenas números, com DDD">
            <input type="email" name="email" placeholder="E-mail" required>
            <select name="cargo" required>
                <option value="">Selecione o cargo</option>
                <option value="gerente">Gerente</option>
                <option value="coordenador">Coordenador</option>
                <option value="especialista">Especialista</option>
                <option value="tecnico">Técnico</option>
            </select>
            <input type="password" name="new_password" placeholder="Senha" required>
            <button type="submit">Registrar</button>
        </form>
        <ul>
            <% tasks.forEach(task => { %>
                <li>
                    <span class="<%= task.done ? 'done' : '' %>" style="flex:1;"><%= task.title %></span>
                    <% if (!task.done) { %>
                        <form action="/tasks/done/<%= task.id %>" method="POST">
                            <button type="submit" title="Concluir"><span>✅</span>Concluir</button>
                        </form>
                    <% } %>
                    <form action="/tasks/delete/<%= task.id %>" method="POST">
                        <button type="submit" title="Excluir"><span>🗑️</span>Excluir</button>
                    </form>
                </li>
            <% }) %>
        </ul>
    </div>
    <div class="container-inicial">
        <h1>Bem-vindo ao Task Manager</h1>
        <p>Gerencie suas tarefas de forma simples e eficiente.</p>
        <button id="btn-register" onclick="document.getElementById('register-modal').style.display='block'">Registre-se</button>
    </div>
    <!-- Modal de registro -->
    <div id="register-modal" style="display:none; position:fixed; top:0; left:0; width:100vw; height:100vh; background:#0008; align-items:center; justify-content:center;">
        <form action="/register" method="POST" style="background:#fff; padding:32px 24px; border-radius:12px; min-width:280px; max-width:90vw; margin:auto; display:flex; flex-direction:column; gap:16px;">
            <h2 style="margin-bottom:12px; color:#2575fc;">Registrar Usuário</h2>
            <input type="text" name="name" placeholder="Nome" required style="padding:10px; border-radius:6px; border:1px solid #ccc;">
            <input type="email" name="email" placeholder="E-mail" required style="padding:10px; border-radius:6px; border:1px solid #ccc;">
            <input type="password" name="password" placeholder="Senha" required style="padding:10px; border-radius:6px; border:1px solid #ccc;">
            <div style="display:flex; gap:8px; justify-content:flex-end;">
                <button type="button" onclick="document.getElementById('register-modal').style.display='none'" style="background:#eee; color:#333;">Cancelar</button>
                <button type="submit" style="background:#2575fc; color:#fff;">Registrar</button>
            </div>
        </form>
    </div>
    <script>
        // Fecha o modal ao clicar fora do formulário
        document.addEventListener('click', function(e) {
            const modal = document.getElementById('register-modal');
            if (modal && e.target === modal) modal.style.display = 'none';
        });
    </script>
</body>
</html>
